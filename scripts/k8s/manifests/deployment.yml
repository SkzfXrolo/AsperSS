apiVersion: apps/v1
kind: Deployment
metadata:
  name: argus-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: argus-web
  template:
    metadata:
      labels:
        app: argus-web
    spec:
      containers:
        - name: web
          image: ghcr.io/review/argus-web:latest
          ports:
            - containerPort: 8000
