apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: argus-web
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: argus-web
  minReplicas: 2
  maxReplicas: 10
