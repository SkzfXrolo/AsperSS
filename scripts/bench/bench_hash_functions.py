from __future__ import annotations\nimport hashlib,json,time\npayload=b'argus'*2000\nN=100000\nresults={}\nfor algo in ('sha256','sha1','md5'):\n    t0=time.perf_counter()\n    for _ in range(N):\n        hashlib.new(algo,payload).digest()\n    results[algo]=time.perf_counter()-t0\nprint(json.dumps({'benchmark':'hash_functions','iterations':N,'results_s':results,'xxhash':'install xxhash to extend'},indent=2))\n
