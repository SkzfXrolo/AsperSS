// Pack48-G benchmark SPEC (no productivo)
// Objetivo: medir pushMove/pushAttack bajo carga y contención.

/*
@BenchmarkMode(Mode.SampleTime)
@OutputTimeUnit(TimeUnit.MICROSECONDS)
@State(Scope.Thread)
public class PacketDataStoreBenchmark {
    @Benchmark
    public void benchmarkPushMove() {
        // state.pushMove(System.currentTimeMillis());
    }

    @Benchmark
    public void benchmarkPushAttack() {
        // state.pushAttack(System.currentTimeMillis());
    }
}
*/
