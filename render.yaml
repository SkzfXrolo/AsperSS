services:
  - type: web
    name: aspers-app
    runtime: python
    rootDir: web_app
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --config gunicorn_config.py app:app
    autoDeploy: true
