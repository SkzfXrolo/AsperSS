================================================================
  ASPERS PROJECTS — PENDIENTES PARA PRÓXIMA SESIÓN
  Última actualización: 22/04/2026
================================================================

── TAREAS PEDIDAS ────────────────────────────────────────────

[x] Crear Vistas móvil
      → Hamburger menu con sidebar deslizante (left: -260px → 0)
      → Overlay para cerrar sidebar al tocar fuera
      → Stats grid apilado en 1 columna en < 540px
      → Subnav colapsado a solo íconos en móvil
      → Sidebar se cierra automáticamente al navegar

[x] Crear aplicación web móvil
      → PWA (Progressive Web App) con manifest.json y service worker
      → Instalable desde el navegador en Android/iOS
      → Notificaciones push cuando llega un scan nuevo
      → Modo offline básico con caché de último scan

[x] Arreglar descargas por link generado por token
      → Fix: nombre MinecraftSSTool.exe → ArgusScanner.exe en BD + possible_paths
      → Fix: condición y nombre del ZIP actualizado
      → Fix: dropdown en panel.html y mensajes de error en panel.js

── MEJORAS DE PANEL WEB ──────────────────────────────────────

[x] Sistema de notificaciones en tiempo real
      → Polling cada 30s detecta scans nuevos
      → Badge rojo en el nav "Escaneos" con conteo de nuevos
      → Toast animado (slide-in) al llegar scan nuevo, clickeable para abrirlo
      → Sonido sutil via Web Audio API (toggle persistido en localStorage)
      → Auto-refresca el dashboard si está activo al detectar scan nuevo

[x] Dashboard con estadísticas globales
      → Gráfico de scans por día (últimos 30 días) ya existente — mejorado
      → Doughnut chart de veredictos: Limpio / Con Hacks / Pendiente con %
      → Top 5 hacks del mes con barras de progreso proporcionales
      → Tiempo promedio de escaneo en el footer
      → Endpoint /api/dashboard/extended con caché de 60s

[x] Perfil de jugador (historial)
      → Click en nombre de máquina (tabla o detalle) abre historial
      → Escaneos Previos muestra stats: total scans, con hacks, limpios
      → Cada scan previo muestra veredicto y diferencia de issues vs anterior
      → viewPlayerProfile() carga el scan más reciente y abre la pestaña

[x] Sistema de notas por scan
      → Tabla scan_notes (PostgreSQL + MySQL, auto-migrada)
      → API GET/POST /api/scans/<id>/notes y DELETE /api/scans/<id>/notes/<note_id>
      → Pestaña "Notas" en la vista de detalle del scan
      → Solo el autor o admin puede eliminar notas

[x] Exportar resultados
      → Exportar a CSV con BOM para Excel (metadatos + todos los hallazgos)
      → Endpoint GET /api/scans/<id>/export/csv
      → Botón "CSV" en la cabecera de la vista de detalle
      → PENDIENTE: exportar a PDF con logo

[x] Gestión de veredictos mejorada
      → Modal con razón obligatoria al marcar "Con Hacks" o "Limpio"
      → Tabla verdict_history registra cada cambio (quién, cuándo, razón)
      → Banner en el detalle muestra el veredicto actual con su razón
      → Botón "Historial" muestra todos los cambios inline
      → API POST /api/scans/<id>/verdict y GET /api/scans/<id>/verdict/history

[x] Filtros y búsqueda avanzada
      → Búsqueda por máquina, username, IP (ILIKE server-side)
      → Filtrar por veredicto: pendiente / limpio / con hacks
      → Filtrar por rango de fecha (desde / hasta)
      → Guardar filtros como presets (localStorage)
      → PENDIENTE: filtrar por país, OS, staff que escaneó

[x] Sistema de permisos de staff
      → Roles: Owner / Admin / Moderador / Helper (jerarquía en auth.py)
      → Helper solo puede ver scans, no cambiar veredictos (403 en backend + oculto en UI)
      → Admin puede crear tokens, Moderador no (403 en backend + botón oculto)
      → Sección "Staff" en sidebar (solo Admin/Owner) con tabla de usuarios y selector de rol
      → API GET /api/staff/users y PUT /api/staff/users/<id>/role
      → STAFF_ROLE inyectado como JS global desde Flask template

── MEJORAS DEL SCANNER .EXE ──────────────────────────────────

[x] Auto-actualización del scanner
      → Al abrir, verificar si hay versión nueva en el servidor (/api/scanner/version)
      → Descargar y reemplazarse automáticamente si hay update (bat script)
      → Mostrar changelog de la nueva versión

[x] Protección anti-detección del scanner
      → Título de ventana cambiado a "Windows Security Health" etc. durante el scan
      → PENDIENTE: ofuscación del binario (PyInstaller + UPX) — requiere build step
      → El jugador no sabe qué está escaneando (ventana tiene nombre genérico)

[x] Captura de pantalla durante el scan
      → Scanner toma screenshot al enviar resultados (PIL.ImageGrab o mss)
      → Envía como base64 JPEG (calidad 50) en el payload de resultados
      → api_server.py almacena en columna screenshot TEXT de la tabla scans
      → Panel muestra la imagen en la pestaña "Captura" del detalle del scan

[x] Mejor detección de Minecraft activo
      → get_minecraft_active_info() en user_info_collector.py: detecta versión, launcher, mods, agentes Java
      → Versión extraída de --version en cmdline de javaw.exe
      → Launcher: Lunar, Badlion, Feather, Forge, Fabric, Quilt, OptiFine, Oficial
      → Mods: lista .jar desde carpeta mods del --gameDir
      → Agentes Java (-javaagent) detectados en cmdline
      → mc_info almacenado como JSON en columna mc_info de scans table
      → Panel muestra versión, launcher, mods y agentes en la sección de detalles del scan

[x] Base de datos de hashes de hacks en la nube
      → Tabla hack_hashes en PostgreSQL (sha256, hack_name, confirmed_count)
      → GET /api/hashes — scanner descarga al iniciar + caché offline en APPDATA
      → POST /api/hashes — admin añade/confirma hashes (upsert)
      → DELETE /api/hashes/<sha256> — admin elimina
      → Detección 100% confiable por hash aunque el archivo esté renombrado

[x] Módulo de detección de macros de mouse/teclado
      → Detectar AutoHotKey procesos activos, scripts .ahk, registro instalación
      → Logitech LGHUB settings.json: keywords click/macro/interval
      → Razer Synapse3/Profiles: JSON/XML con keywords de macro
      → DNS cache: ipconfig /displaydns cruzado con dominios de hacks conocidos
      → Event Log EventID 4616: cambio de fecha del sistema
      → JNA artifacts en %APPDATA%/%TEMP%

[x] Escaneo de memoria de procesos Java
      → _scan_java_process_memory() en ss_forensics.py
      → Detecta -javaagent flags en javaw.exe cruzado con lista de hacks
      → Mapea DLLs cargadas via psutil.memory_maps() buscando keywords sospechosas

[x] Mejorar tiempo total del scan
      → Timeout por unidad reducido a 55-80s (según cores) — antes 300-600s
      → max_depth reducido de 8 a 4 (suficiente para rutas Minecraft relevantes)
      → Fases secundarias: timeout 80s (antes 300s)
      → Executor principal: timeout 85s (antes 600s)
      → Fases A-G ya corren en paralelo (7 workers)

── INFRAESTRUCTURA / BACKEND ─────────────────────────────────
[ ] Bot de Discord oficial
      → Comandos slash: /scan @jugador, /veredicto <id>, /stats
      → Notificación automática en canal cuando llega scan nuevo
      → El staff puede marcar veredicto directamente desde Discord

================================================================
  BUGS CONOCIDOS A REVISAR
================================================================

[ ] La barra de progreso a veces salta de golpe en vez de animar suave
[x] El username del jugador aparece como "No detectado" a veces
      → Fix: soporte minecraftProfile.name (cuentas Microsoft modernas)
      → Fix: cuenta activa prioritizada con activeAccountLocalId
      → Fix: usercache.json ordenado por expiresOn desc
      → Fix: machine_name con fallback HOSTNAME / socket.gethostname()
[ ] En scans muy rápidos (< 30s) el panel no recibe los datos a tiempo
[x] El panel no refresca automáticamente cuando llega un scan nuevo
      → Fix: polling cada 30s + badge + toast + auto-refresh del dashboard
[ ] En resoluciones < 1200px la subnav puede hacer wrap en algunos browsers
[x] REGRESIÓN: detalle de scan no cargaba ningún dato (Archivos: 0, Fecha: N/A, todo vacío)
      → Causa raíz real: query opcional de verdict/screenshot/mc_info fallaba en psycopg2,
        poniendo la conexión en "transaction aborted" — el SELECT de scan_results también fallaba
      → Fix: SAVEPOINT alrededor de query opcional en get_scan() para no abortar la transacción
      → Fix: ALTER TABLE en web_app/db_postgresql.py para verdict, screenshot, mc_info, scan_notes, verdict_history
      → Fix: banner "Detectado" con display:flex !important → display:flex (ya no override JS)
[x] Resultados llegan antes de que el scanner termine (scans con status='running' disparaban notificación)
      → Fix: poll solo notifica badge/toast para scans 'completed'; running van a _pendingRunning
      → Fix: viewScanDetails muestra "Scan en progreso..." si status='running'
      → Fix: _resultBadge muestra badge "⏳ Escaneando" para running scans

================================================================
Reediseñar todo el sistema de llegada de datos de scaneos a la página porque se ve
infinito



Si es posible hacer que el scanner haga todo esto además de lo que ya hace

CMD:
-sc query dps (Si En El Estado Aparece como STOPPED Esta Usando Un GhostClient o ha deshabilitado un proceso de windows)
-ipconfig/displaydns (Muestra las paginas abiertas)
-tasklist (Muestra los .exe Ejecutados)
-dir /b/s *.exe (muestra todos los .exe que tiene el user)
-dir /b/s *.jar (cumple la misma funcion que el comando de arriba pero con .jar)Â¨
- archivos newnames (cd c:\users%username%): FORFILES /M *.exe /S /D +04/08/2021 /C "cmd /C echo @fdate @file @path" (define la fecha a la que quieras buscar el archivo)
- archivos borrados (cd c:\windows\system32): fsutil usn readjournal c: csv | findstr /i /c:.exe | findstr /i /c:0x80000200 >> deletedfiles.txt
- archivos  creados (cd c:\windows\system32): fsutil usn readjournal c: csv | findstr /i /c:.exe | findstr /i /c:0x00000100 >> createdfiles.txt
- archivos oldnames (cd c:\windows\system32): fsutil usn readjournal c: csv | findstr /i /c:.exe | findstr /i /c:0x00001000 >> oldnamefiles.txt
- archivos newnames (cd c:\windows\system32): fsutil usn readjournal c: csv | findstr /i /c:.exe | findstr /i /c:0x00002000 >> newnamefiles.txt
- ver ruta de archivo(los numeros mas largos, el segundo): fsutil file queryFileNameById C:\ 0x(numeros)
PROCCES HACKER: 
-vamos al explorer.exe , le damos a propeties , quitamos el "hide free regions" , le damos a "strings" , quitamos el "10" y pones "4" , activas las 3 casillas, le das a "ok" , cuando cargue le das a "filter" "contains case-insensitive" y buscas "pcaclient", ahi clicas en el primer resultado y le das a "save search results", ahora vamos a elementos recientes y abrimos los "search results" y vemos si hay algun hack .
-vamos al explorer.exe , le damos a propeties ,  quitamos el "hide free regions" , le damos a "strings" , quitamos el "10" y pones "4" , activas las 3 casillas, le das a "Ok", cuando cargue le das a "filter" y pones "file://" , cuando cargue le das a filter ""contains case-insensitive" ,y pones en el buscador ".dll" para ver cuales tiene.
WINDOWS + R:
-eventvwr.msc (sirve para ver si cambio la fecha del equipo)
-%appdata%
-entras al "prefetch" buscas "jna" + revisar 
-entras al "%temp%" y miras todas las carpetas , despues buscas "jna" + revisar
-apretas la tecla "windows + r" y en el buscador pones "explorer.exe shell:::{05d7b0f4-2121-4eff-bf6b-ed3f69b894d9}"

REGEDIT:
regedit : current user : software : microsoft : windows nt : current versions : Compatibility asistant : store
Equipo\HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Explorer\ComDlg32\OpenSavePidlMRU\txt          
Equipo\HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows NT\CurrentVersion\AppCompatFlags\Compatibility Assistant\Store

POWERSELL:
EL POWERSELL ES PARACIDO AL CMD PERO NOS PUEDE DAR OTROS RESULTADOS 
-entramos al buscador y pones "gci -recurse -filter *.exe"
-entramos al buscador y pones "gci -recurse -filter *.rar"
-entramos al buscador y pones "gci -recurse -filter *.dll"
-ejecutamos el powersell como "administrador" y buscas  "mmagent" [COMO ADMINISTRADOR]

MACROS LOGITECH (LGHUB):
-windows+r : appdata : Local : LGHUB : Settings.json 
MACROS RAZER:
-C:\ProgramData : Razer : Synapse Accounts

(Si encuentras alguna mejora dale con fe)