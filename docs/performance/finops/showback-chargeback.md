# Showback / Chargeback

- **Showback:** reporte de costo por cliente sin facturación directa.
- **Chargeback:** costo trasladado al cliente Enterprise según contrato.

Fórmula base:
`costo_cliente = base_fija + (scans * costo_scan) + (oracle_evals * costo_eval)`
