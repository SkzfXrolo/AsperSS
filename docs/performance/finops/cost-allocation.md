# Cost Allocation

Modelo tag-based:
- `tenant_id`
- `service`
- `environment`
- `feature`

Reglas:
- todo recurso nuevo debe nacer etiquetado,
- costos no etiquetados se asignan a `shared` y se corrigen en 24h.
