# Quick start monitoring (10 min)

1. Ir a `scripts/monitoring/`.
2. Ejecutar `docker compose -f docker-compose.monitoring.yml up -d`.
3. Abrir Grafana en `:3000`.
4. Cargar dashboards JSON de `grafana-dashboards/`.
