# Retraining Triggers

Retrain cuando:
- drift > threshold,
- performance drop > threshold,
- ventana temporal programada (ej. mensual).

Prioridad:
1. trigger por caída de calidad,
2. trigger por drift crítico,
3. trigger calendarizado.
