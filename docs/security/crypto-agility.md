# Crypto Agility

Objetivo: cambiar algoritmos sin romper contratos.

- versionado (`encryptionV1`, `encryptionV2`).
- migración dual-read/dual-write.
- feature flags y rollback.
- lección histórica: MD5 -> SHA-1 -> SHA-256 exige planes de transición.
