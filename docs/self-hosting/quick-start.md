# Quick start self-hosting (5 minutos)

1. Copiar `docker/.env.example` a `docker/.env` y completar secretos.
2. Ejecutar `cd docker && docker compose up -d`.
3. Verificar salud de servicios (`docker compose ps`).
4. Configurar DNS y certificados TLS.
5. Confirmar acceso web y endpoints clave.
